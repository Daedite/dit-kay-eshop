version: "3.8"

services:
  app:
    container_name: eshop-client-react-dev
#    image: 30e5a67b1a6a
    image: eshop-client-react-dev
    build:
      context: .
      target: development
    volumes:
      - ./src:/app/src
    ports:
      - 3003:3003